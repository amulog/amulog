
[ext]
rules = ipaddr, host

# function names
ipaddr = label_ipaddr
host = label_host


[re]
rules = digit, path, ifname

# regular expressions
digit = ^\d+$
path = ^/[0-9a-z_]+/[0-9a-z/_]+$
ifname = ^eth\d+$

