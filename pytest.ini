[pytest]
# Only the core suite under tests/ runs by default (matching CI, which runs
# `pytest tests/`). The external tests under tests-ext/ require optional
# backends and live servers (e.g. a running MySQL with pymysql installed) and
# must be requested explicitly: `pytest tests-ext/`.
testpaths = tests
